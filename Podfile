# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Distribution-Data-Persistence-App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for device-mgr-mobile
  pod "TextFieldEffects"
  pod "Popover"
  

  target 'Distribution-Data-Persistence-AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Distribution-Data-Persistence-AppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
